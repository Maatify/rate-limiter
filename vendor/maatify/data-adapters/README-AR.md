![Maatify.dev](https://www.maatify.dev/assets/img/img/maatify_logo_white.svg)

---

[![Version](https://img.shields.io/packagist/v/maatify/data-adapters?label=Version&color=4C1)](https://packagist.org/packages/maatify/data-adapters)
[![PHP](https://img.shields.io/packagist/php-v/maatify/data-adapters?label=PHP&color=777BB3)](https://packagist.org/packages/maatify/data-adapters)
[![Build](https://github.com/Maatify/data-adapters/actions/workflows/test.yml/badge.svg?label=Build&color=brightgreen)](https://github.com/Maatify/data-adapters/actions/workflows/test.yml)
[![Monthly Downloads](https://img.shields.io/packagist/dm/maatify/data-adapters?label=Monthly%20Downloads&color=00A8E8)](https://packagist.org/packages/maatify/data-adapters)
[![Total Downloads](https://img.shields.io/packagist/dt/maatify/data-adapters?label=Total%20Downloads&color=2AA9E0)](https://packagist.org/packages/maatify/data-adapters)
[![Stars](https://img.shields.io/github/stars/Maatify/data-adapters?label=Stars&color=FFD43B)](https://github.com/Maatify/data-adapters/stargazers)
[![License](https://img.shields.io/github/license/Maatify/data-adapters?label=License&color=blueviolet)](LICENSE)
[![Status](https://img.shields.io/badge/Status-Stable-success?style=flat-square)]()
[![Code Quality](https://img.shields.io/codefactor/grade/github/Maatify/data-adapters/main)](https://www.codefactor.io/repository/github/Maatify/data-adapters)

[![Changelog](https://img.shields.io/badge/Changelog-View-blue)](CHANGELOG.md)
[![Security](https://img.shields.io/badge/Security-Policy-important)](SECURITY.md)

---

# 📦 maatify/data-adapters
**طبقة موحَّدة لاتصال البيانات والتشخيص (Unified Data Connectivity & Diagnostics Layer)**

---

> 🔗 [English Version 🇬🇧](./README.md)

## 🧭 نظرة عامة

تُوفّر مكتبة **maatify/data-adapters** طبقة موحَّدة وقابلة للتوسّع لإدارة الاتصالات مع مصادر بيانات متعددة — مثل **Redis** و**MongoDB** و**MySQL** — وذلك ضمن نظام تشخيص مركزي مع خاصية **الكشف التلقائي عن بيئة التشغيل**.
تمثّل هذه المكتبة **الطبقة الأساسية للبيانات** في منظومة **Maatify Ecosystem** بأكملها.

---

## ⚙️ التثبيت

```bash
composer require maatify/data-adapters
```

> تتطلّب PHP بإصدار **8.4 أو أحدث**، مع تفعيل إضافات `redis` و`pdo_mysql` و`mongodb` (اختيارية).

---

## 🚀 الاستخدام السريع

يوضّح المثال التالي كيفية تهيئة بيئة التشغيل واستخدام **DatabaseResolver** للوصول إلى المحوّلات (Adapters) المختلفة مثل **Redis** و**MySQL**:

```php
use Maatify\DataAdapters\Core\EnvironmentConfig;
use Maatify\DataAdapters\Core\DatabaseResolver;
use Maatify\DataAdapters\Enums\AdapterTypeEnum;

$config   = new EnvironmentConfig(__DIR__);
$resolver = new DatabaseResolver($config);

// Redis Adapter
$redis = $resolver->resolve(AdapterTypeEnum::REDIS);
$redis->connect();

// MySQL Adapter
$mysql = $resolver->resolve(AdapterTypeEnum::MYSQL);
$pdo   = $mysql->getConnection();
```

> يتيح هذا المثال إنشاء الاتصال تلقائيًا بناءً على إعدادات البيئة،
> مع دعم فوري لكل من **Redis** و**MySQL** ضمن نفس طبقة الوصول إلى البيانات.

---

## 🧩 الفحص التشخيصي والتحقق من السلامة

تتضمّن جميع **المحوّلات (Adapters)** آليات **تشخيص ذاتي (Self-Diagnostic)** وتقارير موحَّدة لحالة الاتصال (**Health Reporting**).

```php
use Maatify\DataAdapters\Diagnostics\DiagnosticService;

$diagnostic = new DiagnosticService($config, $resolver);
echo $diagnostic->toJson();
```

**ناتج تجريبي:**

```json
{
  "diagnostics": [
    {"adapter": "redis", "connected": true},
    {"adapter": "mongo", "connected": true},
    {"adapter": "mysql", "connected": true}
  ]
}
```

> تتيح هذه الآلية التأكّد من جاهزية جميع مصادر البيانات واتصالها الفعلي بالنظام في أي وقت.

---

## 🧱 نظرة عامة على البنية المعمارية

```
src/
├─ Core/
│   ├─ EnvironmentConfig.php      # تهيئة بيئة التشغيل
│   ├─ DatabaseResolver.php       # إدارة الربط بالمحوّلات
│   └─ BaseAdapter.php            # الأساس الموحّد لجميع المحوّلات
├─ Adapters/
│   ├─ RedisAdapter.php           # محوّل Redis
│   ├─ PredisAdapter.php          # محوّل Predis الاحتياطي
│   ├─ MongoAdapter.php           # محوّل MongoDB
│   ├─ MySQLAdapter.php           # محوّل MySQL باستخدام PDO
│   └─ MySQLDbalAdapter.php       # محوّل MySQL باستخدام DBAL
├─ Diagnostics/
│   ├─ DiagnosticService.php      # خدمة الفحص التشخيصي
│   ├─ AdapterFailoverLog.php     # سجلّات فشل المحوّلات (Failover Logs)
│   └─ Logger/
│       ├─ FileAdapterLogger.php             # المدوّن المخصّص للمحوّلات
│       └─ Contracts/AdapterLoggerInterface.php  # واجهة المدوّن
```

> تعكس هذه البنية تقسيمًا دقيقًا بين الطبقات الأساسية، والمحوّلات، ووحدات التشخيص لتسهيل الصيانة والتوسّع.

---

## ⚙️ متغيّرات البيئة (Environment Variables)

| المتغيّر                | الوصف                                                                                                 |
|:------------------------|:------------------------------------------------------------------------------------------------------|
| `REDIS_HOST`            | عنوان الخادم أو عنوان الـ IP الخاص بـ Redis. يُستخدم للتخزين المؤقت، وإدارة الطوابير، وأقفال التوزيع. |
| `REDIS_PORT`            | منفذ الاتصال بـ Redis (الافتراضي: 6379).                                                              |
| `REDIS_PASSWORD`        | كلمة مرور Redis (اختياريّة — اتركها فارغة إذا لم تكن هناك مصادقة).                                    |
| `MONGO_HOST`            | عنوان خادم MongoDB لتخزين السجلات والبيانات التاريخية.                                                |
| `MONGO_PORT`            | منفذ اتصال MongoDB (الافتراضي: 27017).                                                                |
| `MONGO_USER`            | اسم مستخدم MongoDB (في حال تفعيل المصادقة).                                                           |
| `MONGO_PASS`            | كلمة مرور MongoDB (في حال تفعيل المصادقة).                                                            |
| `MONGO_DB`              | اسم قاعدة البيانات المستهدفة في MongoDB.                                                              |
| `MYSQL_HOST`            | عنوان خادم MySQL للبيانات التشغيلية والمعاملات.                                                       |
| `MYSQL_PORT`            | منفذ اتصال MySQL (الافتراضي: 3306).                                                                   |
| `MYSQL_USER`            | اسم مستخدم MySQL.                                                                                     |
| `MYSQL_PASS`            | كلمة مرور MySQL (يمكن تركها فارغة لبيئة التطوير المحلية).                                             |
| `MYSQL_DB`              | اسم قاعدة البيانات المستهدفة في MySQL.                                                                |
| `MYSQL_DRIVER`          | نوع سواقة الاتصال (مثل: `dbal` أو `pdo`).                                                             |
| `APP_ENV`               | بيئة التطبيق (`local`، `staging`، `production`).                                                      |
| `LOG_PATH`              | المسار العام لتخزين السجلات (logs).                                                                   |
| `ADAPTER_LOG_PATH`      | المسار المخصّص لسجلات المحوّلات (لكل محوّل على حدة).                                                  |
| `METRICS_ENABLED`       | تفعيل أو تعطيل نظام القياسات (Metrics Collector).                                                     |
| `METRICS_EXPORT_FORMAT` | تنسيق إخراج بيانات القياس (مدعوم: `prometheus`، `json`، `none`).                                      |
| `METRICS_SAMPLING_RATE` | نسبة الطلبات التي تُؤخذ عينات منها لجمع القياسات (من 0.0 إلى 1.0).                                    |


> تُستخدم هذه المتغيّرات لضبط إعدادات الاتصال أثناء التشغيل، دون الحاجة لتعديل الشيفرة المصدرية.

---

## 🧪 الاختبارات (Testing)

```bash
vendor/bin/phpunit
```

**نسبة التغطية:**  >  87%
**الحالة:** ✅ جميع الاختبارات ناجحة (تشغيل، تشخيص، فشل احتياطي)

> تم اختبار جميع المكوّنات بما في ذلك آليات الاسترداد (Fallback) والتشخيص الذاتي للتأكّد من ثبات النظام.

---

#### 🧠 مثال لملف البيئة `.env`

```env
# ----------------------------------------------------------
# 🔴 إعدادات محوّل Redis
# ----------------------------------------------------------
# إعدادات اتصال Redis المستخدَمة للتخزين المؤقت (caching)، والطوابير (queueing)، وأقفال التوزيع (distributed locks).
REDIS_HOST=127.0.0.1#          # عنوان الخادم أو الـ IP الخاص بـ Redis
REDIS_PORT=6379#               # منفذ اتصال Redis (الافتراضي: 6379)
REDIS_PASSWORD=#               # كلمة المرور (اختياري، اتركها فارغة إذا لم تكن المصادقة مفعّلة)


# ----------------------------------------------------------
# 🟢 إعدادات محوّل MongoDB
# ----------------------------------------------------------
# تفاصيل اتصال MongoDB لتخزين سجلات النشاطات (activity logs) والبيانات التاريخية.
MONGO_HOST=127.0.0.1#          # عنوان الخادم أو الـ IP الخاص بـ MongoDB
MONGO_PORT=27017#              # منفذ اتصال MongoDB (الافتراضي: 27017)
MONGO_USER=#                   # اسم المستخدم (إن كانت المصادقة مفعّلة)
MONGO_PASS=#                   # كلمة المرور (إن كانت المصادقة مفعّلة)
MONGO_DB=maatify_dev#          # اسم قاعدة البيانات المستهدفة


# ----------------------------------------------------------
# 🔵 إعدادات محوّل MySQL
# ----------------------------------------------------------
# بيانات اعتماد MySQL لمعالجة البيانات التشغيلية، التحليلية، والنسخ الاحتياطي (fallbacks).
MYSQL_HOST=127.0.0.1#          # عنوان خادم MySQL أو الـ IP
MYSQL_PORT=3306#               # منفذ اتصال MySQL (الافتراضي: 3306)
MYSQL_USER=root#               # اسم المستخدم
MYSQL_PASS=#                   # كلمة المرور (اتركها فارغة في بيئة التطوير المحلية)
MYSQL_DB=maatify_dev#          # اسم قاعدة البيانات المستهدفة
MYSQL_DRIVER=dbal#             # نوع السواقة (مثل: dbal، pdo)


# ----------------------------------------------------------
# ⚙️ إعدادات عامة للتطبيق
# ----------------------------------------------------------
APP_ENV=local#                  			# بيئة التطبيق (local، staging، production)
LOG_PATH=storage/logs#          			# المسار العام لتخزين السجلات (logs)
ADAPTER_LOG_PATH=storage/adapter_logs#    	# المسار الخاص بسجلات المحوّلات (لكل سواقة على حدة)


# ----------------------------------------------------------
# 📊 إعدادات القياسات والمراقبة (Metrics & Observability)
# ----------------------------------------------------------
# التحكم في جمع بيانات الأداء (telemetry) وتحديد تنسيق التصدير.
METRICS_ENABLED=true#           	# تفعيل أو تعطيل مجمّع القياسات (metrics collector)
METRICS_EXPORT_FORMAT=prometheus#   # التنسيقات المدعومة: prometheus، json، none
METRICS_SAMPLING_RATE=1.0#     		# نسبة الطلبات المأخوذة كعينات للقياس (من 0.0 إلى 1.0)

```

> يوضّح المثال أعلاه الإعدادات الأساسية لتشغيل Redis وتحديد مسار السجلات التشخيصية.

---

#### 💡 Usage Example

```php
use Maatify\DataAdapters\Core\EnvironmentConfig;
use Maatify\DataAdapters\Core\DatabaseResolver;
use Maatify\DataAdapters\Enums\AdapterTypeEnum;
use Maatify\DataAdapters\Core\Exceptions\ConnectionException;

$config   = new EnvironmentConfig(__DIR__);
$resolver = new DatabaseResolver($config);
$redis    = $resolver->resolve(AdapterTypeEnum::REDIS);

try {
    $redis->connect();
    $redis->set('session:123', 'active');
} catch (ConnectionException $e) {
    // Automatically falls back to PredisAdapter
    echo "⚠️ RedisAdapter failed using phpredis — switching to PredisAdapter.";
}
```

---

## 📈 قابلية المراقبة والقياس (Observability & Metrics)

ابتداءً من **المرحلة السابعة (Phase 7)**، تُقدّم مكتبة **maatify/data-adapters** طبقةً متكاملةً من **القياسات والتليمتري (Telemetry & Metrics)**،
لتوفير مراقبة فورية وتحليلات أداء دقيقة لجميع المحوّلات (**Redis**، **MongoDB**، **MySQL**).

---

### ⚙️ المزايا الأساسية

| الميزة                         | الوصف                                                                                               |
|:-------------------------------|:----------------------------------------------------------------------------------------------------|
| **AdapterMetricsCollector**    | يجمع بيانات التأخير الزمني (latency) ومعدلات النجاح والفشل أثناء التشغيل.                           |
| **AdapterMetricsMiddleware**   | يغلّف عمليات المحوّلات ويقيس زمن التنفيذ تلقائيًا.                                                  |
| **PrometheusMetricsFormatter** | يصدّر القياسات بتنسيق متوافق مع **Prometheus** لعرضها في لوحات المراقبة.                            |
| **PSR-Logger Integration**     | يوجّه سجلات التأخير والفشل عبر مكتبة [`maatify/psr-logger`](https://github.com/Maatify/psr-logger). |
| **Grafana Ready**              | يمكن عرض البيانات مباشرة في **Grafana** أو في **maatify/admin-dashboard**.                          |

---

### 🧩 مثال على الاستخدام

```php
use Maatify\DataAdapters\Telemetry\{
    AdapterMetricsCollector,
    PrometheusMetricsFormatter
};

$collector = AdapterMetricsCollector::instance();

// تسجيل بيانات الأداء بعد أي عملية للمحوّل
$collector->record('redis', 'set', latencyMs: 2.15, success: true);

// توليد مخرجات Prometheus
$formatter = new PrometheusMetricsFormatter($collector);
header('Content-Type: text/plain');
echo $formatter->render();
```

**نموذج مخرجات Prometheus**

```
# HELP adapter_latency_avg Average adapter latency (ms)
# TYPE adapter_latency_avg gauge
adapter_latency_avg{adapter="redis"} 2.15
adapter_success_total{adapter="redis"} 1
adapter_fail_total{adapter="redis"} 0
```

---

### ⚙️ إعدادات البيئة (.env Configuration)

```env
METRICS_ENABLED=true
METRICS_EXPORT_FORMAT=prometheus
METRICS_SAMPLING_RATE=1.0
ADAPTER_LOG_PATH=/var/logs/maatify/adapters/
```

> يمكن الوصول إلى القياسات من خلال المسار `/metrics`
> أو مباشرة من **maatify/admin-dashboard**.
> لمزيد من الأمثلة، يُرجى مراجعة الملف
> [`docs/examples/README.telemetry.md`](docs/examples/README.telemetry.md).

---

### 🧱 الخلاصة

تُوفّر طبقة **المراقبة والتليمتري (Observability Layer)** رؤيةً عميقة لأداء المحوّلات،
مع دعمٍ مباشرٍ لتكامل **Prometheus** و**Grafana**،
وبذلك تكتمل منظومة الموثوقيّة (Reliability Stack) التي تم تقديمها في المراحل السابقة.

---

### 🔗 التكامل مع مكتبة maatify/bootstrap

تتوافق مكتبة **maatify/data-adapters** بشكلٍ كامل مع
[`maatify/bootstrap`](https://github.com/Maatify/bootstrap)،
والتي تتولّى عملية **التهيئة التلقائية (Automatic Initialization)** و**حقن الاعتمادية (Dependency Injection)**
لكافة المحوّلات المسجَّلة من خلال كائن الحاوية الموحّد `Container`.

---

#### ⚙️ التسجيل التلقائي (Auto-Registration)

بمجرد تثبيت مكتبة **maatify/bootstrap**،
يتم تسجيل جميع المحوّلات تلقائيًا أثناء مرحلة **التهيئة (Bootstrap Phase)**:

```php
use Maatify\Bootstrap\Bootstrap;
use Maatify\DataAdapters\Core\DatabaseResolver;
use Maatify\DataAdapters\Core\EnvironmentConfig;

$bootstrap = new Bootstrap();
$container = $bootstrap->container();

// استدعاء المحوّلات من أي جزء في النظام:
$config   = $container->get(EnvironmentConfig::class);
$resolver = $container->get(DatabaseResolver::class);
$redis    = $resolver->resolve('redis');
```

> لا حاجة لأي إعداد يدوي — إذ يقوم **maatify/bootstrap** بتحميل متغيّرات البيئة `.env` تلقائيًا،
> وتصبح جميع آليات **التشخيص (Diagnostics)** و**الفشل الاحتياطي (Failover)** و**الاسترداد الذاتي (Recovery)**
> متاحةً بشكل فوري داخل النظام.

---

#### 🧩 الاستخدام ضمن مكتبات Maatify الأخرى

| الوحدة (Module)             | التكامل (Integration)                                                       |
|:----------------------------|:----------------------------------------------------------------------------|
| **maatify/rate-limiter**    | يستخدم `RedisAdapter` (سواء phpredis أو predis) للتحكم في معدل الطلبات.     |
| **maatify/security-guard**  | يتصل عبر `MySQLAdapter` للتحقق من بيانات الاعتماد (Credentials).            |
| **maatify/mongo-activity**  | يعتمد على `MongoAdapter` لتخزين السجلات الهيكلية للأحداث.                   |
| **maatify/common-security** | يقرأ المحوّلات من خلال الحاوية المشتركة (Shared Container).                 |
| **maatify/psr-logger**      | يحقن `FileAdapterLogger` أو أي Logger متوافق مع PSR لتسجيل أنشطة المحوّلات. |

---

#### 🧠 تدفّق الإعداد الموحّد (Unified Configuration Flow)

تُدار جميع إعدادات الاتصال من خلال ملف `.env` واحد مشترك بين جميع المشاريع:

```env
# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# MongoDB
MONGO_HOST=127.0.0.1
MONGO_PORT=27017
MONGO_USER=
MONGO_PASS=
MONGO_DB=maatify_dev

# MySQL
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASS=
MYSQL_DB=maatify_dev
MYSQL_DRIVER=dbal

# Logs
ADAPTER_LOG_PATH=/var/logs/maatify/adapters/
```

> يمكن لأي مكتبة ضمن منظومة **Maatify** طلب اتصال بقاعدة بيانات عبر **الحاوية (Container)** مباشرة،
> دون الحاجة إلى إعدادات مكرّرة أو تخزين بيانات اعتماد منفصلة.

---

🧱 تعمل مكتبة **maatify/data-adapters** كـ **الطبقة المركزية للبيانات** في المنظومة،
إذ تربط بين اتصالات **Redis** و**MySQL** و**MongoDB** ضمن واجهة موحّدة،
مع دعم كامل للتشخيص الموحّد (Unified Diagnostics)
واتصال **Redis** الموحّد الذي يتيح استخدام **Predis** كخيار احتياطي عبر جميع مشاريع **Maatify.dev**.

---

## 🧭 خارطة التطوير (Development Roadmap)

| المرحلة | العنوان                                                                   | الحالة    |
|:--------|:--------------------------------------------------------------------------|:----------|
| 1       | إعداد بيئة التشغيل (Environment Setup)                                    | ✅ مكتملة  |
| 2       | الواجهات الأساسية والبنية التحتية (Core Interfaces & Base Structure)      | ✅ مكتملة  |
| 3       | تنفيذ المحوّلات (Adapter Implementations)                                 | ✅ مكتملة  |
| 3.5     | اختبارات أولية للمحوّلات (Adapter Smoke Tests)                            | ✅ مكتملة  |
| 4       | طبقة التشخيص والتحقق من السلامة (Health & Diagnostics Layer)              | ✅ مكتملة  |
| 4.1     | تحسين سجلّات الفشل الهجين (Hybrid AdapterFailoverLog Enhancement)         | ✅ مكتملة  |
| 4.2     | تجريد نظام التسجيل عبر حقن الاعتمادية (Adapter Logger Abstraction via DI) | ✅ مكتملة  |
| 5       | التكامل والاختبارات الموحدة (Integration & Unified Testing)               | ✅ مكتملة  |
| 7       | الفشل المستمر وطبقة القياس (Persistent Failover & Telemetry)              | ✅ مكتملة  |
| 8       | التوثيق والإصدار (Documentation & Release)                                | ✅ مكتملة  |


---

### 🧱 المرحلة الأولى — إعداد بيئة التشغيل (Phase 1 — Environment Setup)

تمّ في هذه المرحلة التمهيدية وضع الأساس لمشروع **`maatify/data-adapters`**،
بما في ذلك إعداد **Composer**، وتهيئة خدمات **Docker**، وضبط **اختبارات PHPUnit**،
إضافةً إلى أتمتة التكامل المستمر (**CI Automation**) عبر GitHub Actions.

---

**أهم الإنجازات**

* تهيئة مشروع Composer مع إضافة الاعتماد على مكتبة `maatify/common`
* تعريف نظام التحميل التلقائي **PSR-4** تحت المسار `Maatify\\DataAdapters\\`
* إضافة ملف `.env.example` يتضمّن إعدادات **Redis / Mongo / MySQL**
* إعداد خدمات **Docker** عبر ملف `docker-compose.yml`
* إنشاء منظومة اختبارات **PHPUnit** وخط سير اختبارات GitHub Actions

---

**التحقق من التنفيذ**

✅ نظام التحميل التلقائي يعمل بنجاح
✅ اختبارات PHPUnit ناجحة
✅ حاويات Docker تعمل بشكل سليم
✅ التكامل المستمر (CI) تم التحقق من فعاليته

---

📄 للاطّلاع على التفاصيل الكاملة:
[`docs/phases/README.phase1.md`](docs/phases/README.phase1.md)

---

### 🧱 المرحلة الثانية — الواجهات الأساسية والبنية التحتية (Phase 2 — Core Interfaces & Base Structure)

في هذه المرحلة تمّ تقديم **البنية المعمارية الأساسية** والواجهات الموحَّدة التي تشغّل
جميع محوّلات البيانات ضمن **طبقة البيانات الخاصة بمنظومة Maatify**.

---

**أهم الإنجازات**

* تعريف الواجهتين `AdapterInterface` و`BaseAdapter` لتوحيد منطق العمل المشترك بين جميع المحوّلات
* إضافة الاستثناءات `ConnectionException` و`FallbackException` لتمكين إدارة الأخطاء بطريقة منظّمة
* تنفيذ فئة `EnvironmentConfig` لتحميل ملف `.env` بشكل آمن
* تقديم فئة `DatabaseResolver` للقيام بعملية **التحليل التلقائي للمحوّلات (Auto Adapter Resolution)**
* تفعيل خاصية **الكشف التلقائي عن بيئة التشغيل** لكل من Redis وMongo وMySQL

---

**التحقق من التنفيذ**

✅ مساحات الأسماء (Namespaces) تعمل بشكل صحيح
✅ تهيئة `BaseAdapter` تمت بنجاح
✅ ملف `.env` تم تحميله بنجاح

---

📄 للاطّلاع على التفاصيل الكاملة:
[`docs/phases/README.phase2.md`](docs/phases/README.phase2.md)

---

### 🧱 المرحلة الثالثة — تنفيذ المحوّلات (Phase 3 — Adapter Implementations)

شهدت هذه المرحلة تطوير وتنفيذ **المحوّلات الوظيفية الأساسية** لجميع قواعد البيانات المدعومة —
**Redis**، **MongoDB**، و**MySQL** — مع دعم كامل لآلية **الفشل الاحتياطي (Fallback)**
و**تجريد السواقة (Driver Abstraction)** لضمان المرونة والاستقرار.

---

**أهم الإنجازات**

* تطوير `RedisAdapter` باستخدام **phpredis** بشكل افتراضي،
  مع التبديل التلقائي إلى `PredisAdapter` عند غياب الامتداد الأصلي.
* تنفيذ `MongoAdapter` باستخدام برنامج التشغيل الرسمي لـ **MongoDB**.
* إضافة كلٍّ من `MySQLAdapter` (باستخدام PDO) و`MySQLDbalAdapter` (باستخدام Doctrine DBAL).
* تفعيل آلية **الكشف التلقائي عن السواقة (Automatic Driver Detection)** عبر `DatabaseResolver`.
* تضمين معالجة مرنة لإعادة الاتصال (Reconnect) والإيقاف الآمن (Graceful Shutdown).

---

**التحقق من التنفيذ**

✅ اختبار فشل Redis والتحوّل التلقائي إلى Predis بنجاح
✅ التحقق من التحميل التلقائي (Autoload) لجميع الفئات
✅ إضافة اقتراحات Composer بشكل صحيح

---

📄 للاطّلاع على التفاصيل الكاملة:
[`docs/phases/README.phase3.md`](docs/phases/README.phase3.md)

---

### 🧱 المرحلة 3.5 — توسيع اختبارات المحوّلات المبدئية (Phase 3.5 — Adapter Smoke Tests Extension)

قدّمت هذه المرحلة **اختبارات بنيوية خفيفة (Lightweight Structural Tests)** لجميع المحوّلات،
بهدف التحقق من سلامة التحميل التلقائي (**Autoloading Integrity**) واتساق الأساليب (**Method Consistency**)
دون الحاجة إلى اتصالات فعلية بقواعد البيانات.

---

**أهم الإنجازات**

* إنشاء اختبارات `PredisAdapterTest`، و`MongoAdapterTest`، و`MySQLAdapterTest`.
* التحقق من توافق التحميل التلقائي **PSR-4** مع بنية واجهات المحوّلات.
* نجاح تنفيذ حزمة اختبارات **PHPUnit** بنتيجة **4 اختبارات / 10 تأكيدات (Assertions)**.
* آمنة للتشغيل ضمن بيئة **CI** — دون الحاجة إلى أي تبعيات خارجية.

---

**التحقق من التنفيذ**

✅ جميع المحوّلات تُحمّل تلقائيًا بنجاح
✅ البنية الداخلية تم التحقق من صحتها
✅ خط سير التكامل المستمر (CI Pipeline) مستقر

---

📄 للاطّلاع على التفاصيل الكاملة:
[`docs/phases/README.phase3.5.md`](docs/phases/README.phase3.5.md)

---

### 🧱 المرحلة الرابعة — طبقة التشخيص والتحقق من السلامة (Phase 4 — Health & Diagnostics Layer)

في هذه المرحلة تمّ تقديم نظام **المراقبة الذاتية والتقارير الصحية (Self-Diagnostic Monitoring & Health Reporting)**
لجميع المحوّلات، مع دعم مخرجات **JSON** لحظية متوافقة مع لوحة الإدارة
**`maatify/admin-dashboard`** لعرض حالة الاتصالات بشكل فوري ومركزي.

---

**أهم الإنجازات**

* تنفيذ الدالة `healthCheck()` في جميع المحوّلات (**Redis / Predis / Mongo / MySQL**).
* إضافة الفئة `DiagnosticService` لتوليد تقارير الحالة بصيغة **JSON** موحّدة.
* تطوير فئة `AdapterFailoverLog` لتتبّع حالات فشل الاتصال أو التحويل الاحتياطي (**Fallback Failures**).
* إنشاء نقطة نهاية `/health` للفحص الداخلي والتشخيص في الزمن الحقيقي.
* دمج `AdapterTypeEnum` ضمن طبقة التشخيص لتحديد نوع المحوّل أثناء التقارير.

---

**التحقق من التنفيذ**

✅ مخرجات JSON تم التحقق من صحتها
✅ سجلات المحوّلات تعمل بشكل سليم
✅ توافق التعداد (Enum Compatibility) مؤكد

---

📄 للاطّلاع على التفاصيل الكاملة:
[`docs/phases/README.phase4.md`](docs/phases/README.phase4.md)

---

### 🧱 المرحلة 4.1 — تحسين سجلّ الفشل الهجين (Hybrid AdapterFailoverLog Enhancement)

تمّ في هذه المرحلة **إعادة هيكلة (Refactor)** فئة **`AdapterFailoverLog`** لتصبح **مسجِّلًا هجينًا (Hybrid Logger)**،
قادرًا على العمل بأسلوبين: **ثابت (Static)** و**قائم على الكائن (Instance-Based)**،
مع دعم ضبط المسار عبر متغيّر البيئة `.env`.

---

**أهم الإنجازات**

* استبدال المسار الثابت بآلية **تحديد ديناميكي أثناء التشغيل (Dynamic Runtime Resolution)**.
* إضافة مُنشئ (Constructor) يسمح بتحديد مسار مخصّص اختياريًا.
* دمج متغيّر البيئة `ADAPTER_LOG_PATH` لتحديد موقع السجلات.
* إنشاء المجلدات تلقائيًا عند أول عملية كتابة في السجل.
* الحفاظ على **التوافق التام مع الاستخدام الثابت السابق (Backward Compatibility)**.
* تهيئة المسجّل لدعم التكامل مع **PSR Logger** في المرحلة السابعة.

---

**التحقق من التنفيذ**

✅ تمّ التحقق من عمل المسارات الافتراضية والمخصصة
✅ إعداد المسار عبر `.env` يعمل بشكل سليم
✅ التوافق مع الإصدارات السابقة مؤكد

---

📄 للاطّلاع على التفاصيل الكاملة:
[`docs/phases/README.phase4.1.md`](docs/phases/README.phase4.1.md)

---

### 🧱 المرحلة 4.2 — تجريد نظام التسجيل عبر حقن الاعتمادية (Adapter Logger Abstraction via DI)

قدّمت هذه المرحلة **نظام تسجيل مهيكل قائم على حقن الاعتمادية (Dependency Injection)**
بديلًا عن الاعتماد الثابت على `AdapterFailoverLog`،
تمهيدًا لتكاملٍ كامل مع أنظمة التسجيل المتوافقة مع **PSR** في **المرحلة السابعة**.

---

**أهم الإنجازات**

* إضافة الواجهة `AdapterLoggerInterface` لتعريف الدالة القياسية `record()` لتسجيل الأحداث.
* تنفيذ الفئة `FileAdapterLogger` التي تستخدم مسار السجلات المعتمد على متغيّر البيئة `.env`.
* إعادة تصميم فئة `DiagnosticService` لقبول **المسجِّل (Logger)** عبر الحقن بدلًا من التهيئة الثابتة.
* الحفاظ على التوافق مع الاستخدام السابق (Backward Compatibility) لضمان عدم كسر التوافق البرمجي.
* التحقق من إنشاء المجلدات ديناميكيًا وإنتاج السجلات في المسار الصحيح.

---

**التحقق من التنفيذ**

✅ نظام الحقن (DI) يعمل بسلاسة
✅ ملفات السجلات تُنشأ وتُحدَّث بشكل صحيح
✅ التكامل مع مكتبة `maatify/psr-logger` مؤكد

---

📄 للاطّلاع على التفاصيل الكاملة:
[`docs/phases/README.phase4.2.md`](docs/phases/README.phase4.2.md)

---

### 🧱 المرحلة الخامسة — التكامل والاختبارات الموحّدة (Phase 5 — Integration & Unified Testing)

قدّمت هذه المرحلة طبقة **اختبارات تكامل موحّدة (Unified Integration Test Layer)**
تربط بين المحوّلات (Adapters) وبقية مكوّنات **منظومة Maatify**،
وذلك للتحقق من قابلية التشغيل البيني (**Interoperability**) وضمان الجاهزية للربط الفعلي مع الوحدات الأخرى.

---

**أهم الإنجازات**

* إنشاء **اختبارات تكامل تجريبية (Mock Integrations)** لكل من:
  `RateLimiter`، و`SecurityGuard`، و`MongoActivity`.
* إعداد **قوالب اختبارات تكامل حقيقية (Real Integration Templates)** بصيغة `.tmp` لتفعيلها لاحقًا.
* توحيد هيكلية الاختبارات ضمن المسار `/tests/Integration` لتسهيل التحقق على مستوى المنظومة الكاملة.
* تضمين اختبارات مزدوجة لسواقت MySQL (باستخدام **PDO** و**DBAL**).
* التحقق من **عزل البيئة (Environment Isolation)** عبر `DatabaseResolver` لضمان اتساق التنفيذ.

---

**التحقق من التنفيذ**

✅ اجتازت جميع اختبارات الـ Mock بنجاح
✅ اختبارات الوحدات الحقيقية جاهزة للتفعيل لاحقًا
✅ الهيكل العام متوافق مع متطلبات CI وجاهز للتشغيل الآلي

---

📄 للاطّلاع على التفاصيل الكاملة:
[`docs/phases/README.phase5.md`](docs/phases/README.phase5.md)

---

### 🧱 المرحلة السابعة — قابلية المراقبة والقياس (Phase 7 — Observability & Metrics)

في هذه المرحلة تمّ تقديم نظام **مراقبة وهيكلة القياسات (Structured Observability & Telemetry)**
لكافة المحوّلات (**Redis**، **MongoDB**، **MySQL**) مع تكاملٍ مباشرٍ مع
**PSR-Logger** و**Prometheus** لتوفير مراقبة فورية للأداء وتحليل في الزمن الحقيقي.

---

**أهم الإنجازات**

* إضافة الفئات `AdapterMetricsCollector`، و`PrometheusMetricsFormatter`، و`AdapterMetricsMiddleware` لتجميع وتحليل القياسات.
* دمج **PSR-Logger** لالتقاط السياقات الخاصة بعمليات المحوّلات.
* إنشاء نقطة النهاية `/metrics` لتوليد بيانات متوافقة مع **Prometheus** لعرضها في أنظمة المراقبة.
* تحقيق تغطية اختبارية تقارب **90٪** مع **زمن إضافي أقل من 0.3 ملّي ثانية** لكل عملية.

---

**التحقق من التنفيذ**

✅ جميع الاختبارات اجتازت بنجاح
✅ مخرجات Prometheus تم التحقق من صحتها
✅ تكامل القياسات والمراقبة مؤكد

---

📄 للاطّلاع على التفاصيل الكاملة:
[`docs/phases/README.phase7.md`](docs/phases/README.phase7.md)

---

### 🧱 المرحلة الثامنة — التوثيق والإصدار (Phase 8 — Documentation & Release)

في هذه المرحلة الختامية تمّ **دمج نتائج جميع المراحل السابقة**
وتهيئة المكتبة للإصدار العام على منصة **Packagist** بشكل رسمي.

---

**أهم الإنجازات**

* دمج جميع ملفات التوثيق الخاصة بالمراحل في ملف واحد موحّد: `/docs/README.full.md`.
* إضافة ملفات المشروع الأساسية: `CHANGELOG.md`، و`VERSION`، و`LICENSE`، و`SECURITY.md`.
* تحديث ملف `composer.json` إلى الإصدار **1.0.0** مع تضمين بيانات الإصدار (Release Metadata).
* التحقق من تكامل المكتبة مع المشاريع التابعة:
  **`maatify/security-guard`**، و**`maatify/rate-limiter`**، و**`maatify/mongo-activity`**.
* إنشاء الوسم `v1.0.0` والتحقق من جاهزية الأنظمة (CI / Packagist) للإطلاق النهائي.

---

**التحقق من التنفيذ**

✅ جميع الاختبارات والتوثيقات اجتازت بنجاح
✅ نسبة التغطية تجاوزت ‎90٪‎
✅ جاهزية كاملة للنشر على Packagist

---

📄 للاطّلاع على التفاصيل الكاملة:
[`docs/phases/README.phase8.md`](docs/phases/README.phase8.md)

---

## 🔗 المكتبات المرتبطة ضمن منظومة Maatify

* [maatify/common](https://github.com/Maatify/common)
* [maatify/psr-logger](https://github.com/Maatify/psr-logger)
* [maatify/bootstrap](https://github.com/Maatify/bootstrap)
* [maatify/rate-limiter](https://github.com/Maatify/rate-limiter)
* [maatify/security-guard](https://github.com/Maatify/security-guard)
* [maatify/mongo-activity](https://github.com/Maatify/mongo-activity)

> تمثل هذه المكتبات المنظومة التكميلية لمشروع **Maatify**،
> حيث تتكامل جميعها لتوفير بنية تحتية موحّدة وآمنة لإدارة البيانات، التسجيل، والأمان.

---

## 🪪 الرخصة (License)

**[رخصة MIT](LICENSE)** © [Maatify.dev](https://www.maatify.dev)
يمكنك استخدام هذه المكتبة وتعديلها وتوزيعها بحرية، بشرط الإشارة إلى المصدر.

---

📘 **معلومة:**
رخصة **MIT** تُعد من أكثر الرخص البرمجية مرونةً،
وتتيح استخدام الكود في المشاريع المفتوحة أو التجارية دون قيود مع الحفاظ على حقوق النشر الأصلية.
---

> 🔗 **للاطّلاع على التوثيق الكامل وملاحظات الإصدار:**
> راجع الملف [/docs/README.full.md](docs/README.full.md)

---

## 🧱 المؤلفون والشكر (Authors & Credits)

تنتمي هذه المكتبة إلى **المنظومة الأساسية لـ Maatify.dev (Maatify.dev Core Ecosystem)**
وقد تم تصميمها وتطويرها تحت الإشراف التقني لـ:

**👤 Mohamed Abdulalim** — *Backend Lead & Technical Architect*

**المسؤول التقني للبنية التحتية الخلفية لمنظومة Maatify Backend Infrastructure**،
والمسؤول عن التصميم العام للمكتبات الأساسية وتوحيد المعايير التقنية
عبر جميع الوحدات الخلفية ضمن منظومة **Maatify**.
🔗 [www.Maatify.dev](https://www.maatify.dev) | ✉️ [mohamed@maatify.dev](mailto:mohamed@maatify.dev)

---

**🤝 المساهمون (Contributors):**
فريق **Maatify.dev Engineering Team** والمساهمون من مجتمع البرمجيات مفتوحة المصدر
الذين يساهمون باستمرار في تحسين واختبار وتوسيع قدرات هذه المكتبة
عبر العديد من مشاريع **Maatify**.

---

> 🧩 يمثّل هذا المشروع جهدًا هندسيًا موحّدًا بإشراف **Mohamed Abdulalim**،
> لضمان أن تشترك جميع مكوّنات منظومة Maatify الخلفية في أساس موثوق وآمن وسهل الصيانة.

---

<p align="center">
  <sub><span style="color:#777">بُنيَ ❤️ بواسطة <a href="https://www.maatify.dev">Maatify.dev</a> — منظومة موحَّدة لمكتبات PHP الحديثة</span></sub>
</p>

---
