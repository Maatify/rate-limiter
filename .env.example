APP_ENV=testing

# Redis Configuration (local)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# MongoDB Configuration (local)
MONGO_URI=mongodb://127.0.0.1:27017
MONGO_DB=maatify_rate_limiter

# MySQL Configuration (local)
MYSQL_HOST=127.0.0.1
MYSQL_DATABASE=maatify_rate_limiter
MYSQL_USER=root
MYSQL_PASSWORD=

# -----------------------------------------------------------------------------
# ⚙️ إعدادات محدد معدل الطلبات (Rate Limiter Configuration)
# -----------------------------------------------------------------------------
#
# GLOBAL_RATE_LIMIT
#   🔹 أقصى عدد محاولات مسموح بها من نفس الـ IP أو المستخدم خلال نافذة الوقت.
#   النوع: رقم صحيح (integer)
#   مثال: 5 → خمس محاولات في الدقيقة الواحدة (إذا كانت النافذة 60 ثانية)
#
#   ➤ Maximum number of allowed actions per IP or user identity
#     within the defined GLOBAL_RATE_WINDOW.
#     Type: integer
#     Example: 5 → 5 attempts allowed per window.
#
# -----------------------------------------------------------------------------
#
# GLOBAL_RATE_WINDOW
#   🔹 طول فترة القياس بالثواني. بعد انتهاء هذه المدة يتم تصفير العداد.
#   النوع: رقم صحيح يمثل الثواني
#   مثال: 60 → دقيقة واحدة
#
#   ➤ Time window in seconds during which actions are counted.
#     After this period, the counter resets automatically.
#     Type: integer (seconds)
#     Example: 60 → One-minute window.
#
# -----------------------------------------------------------------------------
#
# BACKOFF_BASE
#   🔹 الأساس الرياضي لحساب التأخير التصاعدي (Exponential Backoff).
#     عند كل تجاوز للحد، تتضاعف مدة الانتظار بناءً على هذا الأساس.
#     الصيغة: BACKOFF_BASE ** عدد مرات التجاوز (مع حد أقصى BACKOFF_MAX)
#   النوع: رقم (float أو integer ≥ 1)
#   مثال: 2 → التأخير يتضاعف (2، 4، 8، 16، …)
#
#   ➤ Exponential backoff multiplier (the "power" base).
#     Delay grows exponentially for each repeated violation.
#     Formula: backoff_seconds = min(BACKOFF_BASE ** n, BACKOFF_MAX)
#     Type: number (float or integer ≥ 1)
#     Example: 2 → Delays double each violation.
#
# -----------------------------------------------------------------------------
#
# BACKOFF_MAX
#   🔹 الحد الأقصى لمدة التأخير (بالثواني).
#     يمنع فترات انتظار طويلة جدًا أو لا نهائية.
#   النوع: رقم صحيح (بالثواني)
#   مثال: 3600 → ساعة واحدة كحد أقصى
#
#   ➤ Maximum allowed backoff duration in seconds.
#     Prevents excessively long or infinite wait times.
#     Type: integer (seconds)
#     Example: 3600 → One hour maximum wait time.
#
# -----------------------------------------------------------------------------
#
# 💡 نصائح / Tips:
#   - استخدم قيمًا صغيرة لاختبار الـ login و OTP (مثل LIMIT=5, WINDOW=60).
#   - ارفع القيم في واجهات API العامة ذات التحميل العالي.
#   - BACKOFF_BASE=2 مناسب لمعظم الحالات لأنه يجعل التأخير يتضاعف بسرعة معتدلة.
#   - BACKOFF_MAX يحمي المستخدم من انتظار مفرط (يفضّل بين 600s إلى 3600s).
#   - تأكد أن النظام يرسل Header باسم Retry-After أو حقل JSON لتوضيح وقت الإعادة.
#
#   - Keep lower values for authentication endpoints (login, OTP).
#   - Use higher limits for read-heavy or public API endpoints.
#   - BACKOFF_BASE=2 provides reasonable exponential control.
#   - BACKOFF_MAX limits how long clients must wait.
#   - Always include “Retry-After” headers in 429 responses.
#
# -----------------------------------------------------------------------------
#
# 🧩 أمثلة مقترحة / Example Profiles:
#
#   # Development / Testing
#   GLOBAL_RATE_LIMIT=5
#   GLOBAL_RATE_WINDOW=60
#   BACKOFF_BASE=2
#   BACKOFF_MAX=60
#
#   # Production (Login / OTP endpoints)
#   GLOBAL_RATE_LIMIT=10
#   GLOBAL_RATE_WINDOW=60
#   BACKOFF_BASE=2
#   BACKOFF_MAX=3600
#
#   # Public API (longer window, higher tolerance)
#   GLOBAL_RATE_LIMIT=100
#   GLOBAL_RATE_WINDOW=60
#   BACKOFF_BASE=2
#   BACKOFF_MAX=600
#
# -----------------------------------------------------------------------------
# بعد تعديل القيم، أعد تشغيل التطبيق أو خدمة PHP-FPM لإعادة تحميل المتغيرات.
# After editing this file, restart your app or PHP-FPM to reload environment vars.
# -----------------------------------------------------------------------------

GLOBAL_RATE_LIMIT=200
GLOBAL_RATE_WINDOW=60
BACKOFF_BASE=2
BACKOFF_MAX=3600
